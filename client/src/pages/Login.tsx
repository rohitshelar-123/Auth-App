import Button from "../components/Button";

function Login() {
    return (
        <div>
            <h1>Login</h1>
            <Button text="Login" type="submit" onClick={() => { }} />
        </div>
    )
}

export default Login;